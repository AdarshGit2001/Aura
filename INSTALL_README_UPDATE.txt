AURA README UPDATE

Your working app code does not need to be replaced.

Copy these into the root of your existing aura-hackathon-site project:
  README.md
  .env.example
  docs/screenshots/hero.png
  docs/screenshots/live-demo.png
  docs/screenshots/use-cases.png

The screenshots are cropped from the provided website captures for cleaner GitHub presentation.

After copying the files:
1. Set NEXT_PUBLIC_REPO_URL and NEXT_PUBLIC_PPT_URL in your local .env.local.
2. Keep your real GEMINI_API_KEY only in .env.local.
3. Commit README.md, .env.example, and docs/screenshots/.
4. Do not commit .env.local.
